# CPU Accuracy Notes

Milestone 1 implements a minimal instruction subset without cycle-accurate micro-ops. Cycle counts returned today are placeholders to drive the run loop and tests; the upcoming milestone will introduce per-cycle behavior, page boundary penalties, and interrupt timing details.
